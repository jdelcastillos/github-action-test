name: wf-dotnet

on:
    workflow_dispatch:
    pull_request:
    push:
      branches:
        - main
      
permissions:
  id-token: write
  contents: read
  packages: write
  repository-projects: read

jobs:
  environment:
    name: 🎭 environment
    runs-on: ubuntu-latest
    lista: [
        {
        "variable":"FOLDER_WORK",
        "valor":"vars.FOLDER_PERSONALIZACION_SYNC",
        "archivo":"appspec.yml"
        },
        {
        "variable":"FOLDER_WORK",
        "valor":"vars.FOLDER_PERSONALIZACION_SYNC",
        "archivo":"appspec.yml"
        }
     ]
    matrix:
        value: ${{ fromJson(env.lista) }}
    steps:
      - name: 🎭 Set Environment
        id: set-environment
        run: |
                echo "${{ matrix.value }}"
